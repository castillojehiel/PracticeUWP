<Page
    x:Class="Appointments.Home"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Appointments"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" Loaded="Page_Loaded">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="270" />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>

        <NavigationView x:Name="navViewMainNav" Grid.Column="0" PaneDisplayMode="Auto" OpenPaneLength="270" Grid.ColumnSpan="2" IsBackButtonVisible="Collapsed" SelectionChanged="NavigationView_SelectionChanged" ItemInvoked="NavigationView_ItemInvoked">
            <NavigationView.MenuItems>
                <NavigationViewItem x:Name="navAppointments" Tag="navAppointments" Icon="Calendar" Content="Appointments" />
                <NavigationViewItem x:Name="navPatients" Tag="navAppointments" Icon="People" Content="Patients" />
            </NavigationView.MenuItems>
            <Frame x:Name="frmMain" />
        </NavigationView>

        

    </Grid>
</Page>
